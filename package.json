{
  "name": "testcode",
  "version": "1.0.0",
  "description": "project for storing postman test",
  "directories": {
    "test": "api-testing"
  },
  "scripts": {
    "api-test": "newman run api-testing/jenkins_api_test.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "vikas mishra",
  "license": "ISC",
  "dependencies": {
    "newman": "^4.5.5"
  }
}
